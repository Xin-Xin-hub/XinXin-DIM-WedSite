
<!-- 这是 PCL2 的主页自定义文件。你可以通过修改这个文件来自定义 PCL2 主页：添加文本、打开网页、启动游戏，等等。
     如果你倒腾这个文件把 PCL2 玩炸了，把这个文件直接删除就行了。
     如果你懂得 WPF 的布局文件编写，你甚至可以做出不少的新玩意儿…… -->
<local:MyCard Title="DIM启动器介绍" Margin="0,0,0,15" CanSwap="True">
    <StackPanel Margin="25,40,23,15">
        <TextBlock TextWrapping="Wrap" Margin="0,0,0,4" Foreground="#d5ae21"
                   Text="DIM腐竹不要吃包子呀" />
        <TextBlock TextWrapping="Wrap" Margin="10,0,0,4" Foreground="#f66deb"
                   Text="启动器主页制作xinxin，整合包制作xinxin" />
        <TextBlock TextWrapping="Wrap" Margin="20,0,0,4" Foreground="#1c63c6"
                   Text="背景图制作Dong_Yi_Feng" />
    </StackPanel>
</local:MyCard>

<local:MyCard Title="DIM专用快捷方式" Margin="0,0,0,15" CanSwap="True">
   <StackPanel Margin="20,40,23,15">

           <StackPanel Orientation="Horizontal" Margin="0,10,0,0" HorizontalAlignment="Center">
                      <local:MyButton Margin="0,0,20,2" Width="140" Height="35" HorizontalAlignment="Left" Padding="13,0,13,0"
                                Text="MCBBS" EventType="打开网页" EventData="https://www.mcbbs.net/" />
					  <local:MyButton Margin="0,0,20,2" Width="140" Height="35" HorizontalAlignment="Left" Padding="13,0,13,0"
                                Text="包子主页" EventType="打开网页" EventData="https://space.bilibili.com/19112262/" />
					  <local:MyButton Margin="0,0,20,2" Width="140" Height="35" HorizontalAlignment="Left" Padding="13,0,13,0"
                                Text="Dong_Yi_Feng主页" EventType="打开网页" EventData="https://space.bilibili.com/454717281" />
           </StackPanel>
        </StackPanel>
</local:MyCard>

<local:MyCard Title="ReadMe" Margin="0,0,0,15" CanSwap="True">
    <StackPanel Margin="25,40,23,15">
        <local:MyHint Text="注：以下只对DIM成员所用由xinxin整合的包起作用" />
        <local:MyHint Margin="0,8,0,2" IsWarn="False"
                   Text="ReadMe&#xA; 
                        1.进单人档提示实验性世界怎么办？&#xA;
                        点'确定'or'取消'&#xA;
                        2.有一些mod我不知道怎么使用怎么办？&#xA;
                        先自行百度使用方法，百度无解的，在群里进行友好的询问，切勿什么问题都问&#xA;
                        3.我为什么链接不上服务器？&#xA;
                        先去群公告看是不是更新了版本，检查自己网络环境，&#xA;
                        4.没有白名单怎么办？&#xA;
                        找群管理给你加白名单&#xA;
                        5.我没有正版，怎么使用外置登录？&#xA;
                        请在群文件中找到新周目文件夹，有视频讲解&#xA;
                        6.你们在游戏中输入'!!c'or'!!s'是什么？加密通话？&#xA;
                        这是MCDR的插件，'!!c'切换成旁观者模式or'!!s'切换成生存模式&#xA;
                        7.服务器ip是什么？&#xA;
                        请熟读并背诵'dim.gold'&#xA;
                        8.我遇到的问题这里没有写怎么办？&#xA;
                        请将你的问题发到群里并@xinxin" />
   </StackPanel>
</local:MyCard>

<!-- 

全部通用的属性：
 - Width、Height：设置宽度与高度
 - ToolTip：设置鼠标指向时的提示文本
 - Foreground：调整前景颜色（十六进制字符串，例如 #FF010203 代表 ARGB 中的 255,1,2,3。前两位代表不透明度，FF 为全不透明，00 为全透明）
 - Background：调整背景颜色（使用类似 Background="{DynamicResource ColorBrush5}" 的格式以调用 PCL2 当前的主题颜色，修改末尾的数字编号以改变颜色浓度）
 - Margin：调整外边距（格式为 左,上,右,下。例如 Margin="0,0,0,2" 即为 2 单位的下边距）
 - HorizontalAlignment：设置对齐方式（Left、Center、Right）

TextBlock（纯文本）：
 - Text：设置显示的文本（如果要使用引号等特殊字符，自行百度 XAML 转义）
 - TextWrapping：开启自动换行
 - FontSize：设置字号
 
Image（图片）：
 - Source：要显示的图片的网址

local:MyHint（提示条）：
 - Text：设置显示的文本（&#xA; 代表换行）
 - IsWarn：设置为 True 代表警告样式，False 代表提示样式
 
local:MyButton（按钮）：
 - Text：设置显示的文本
 - Padding：设置内边距，格式与 Margin 一致
 - ColorType：设置颜色主题（Highlight 为当前启动器的主题颜色，Red 为红色，默认为黑色）
 - EventType、EventData：触发特定事件
 
local:MyTextButton（文本按钮）：
 - Text：设置显示的文本
 - EventType、EventData：触发特定事件
 
local:MyCard（卡片）：
 - Title：设置显示的标题文本
 - CanSwap：这张卡片是否可以折叠，True 为是，False 为否
 - IsSwaped：这张卡片是否默认折叠，要求 CanSwap 必须为 True

-->
